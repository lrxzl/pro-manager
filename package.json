{
  "name": "pro-manager",
  "version": "1.0.0",
  "description": "AI Vibecoding Project Management System",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"cd server && node index.js\" \"cd web && npm run dev\"",
    "dev:server": "cd server && node index.js",
    "dev:web": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd server && node index.js",
    "install:all": "npm install && cd server && npm install && cd ../web && npm install",
    "open": "start http://localhost:5174"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
